const sum = (num1, num2) => {
    return num1 + num2
}

const difference = (num1, num2) => {
    return num1 - num2
}

// CommonJS
// module.exports = { sum, difference }

// ES6
// export {sum, difference};